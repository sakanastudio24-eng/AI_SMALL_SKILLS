# Example Structure Review

## Before

API docs are split between `docs/api`, `reference`, and root markdown files.

## After

Move API material under one canonical `docs/api/` tree and replace duplicates with links.
