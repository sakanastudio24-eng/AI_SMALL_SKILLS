# Dashboard Mobile Adaptation Example

## Before

A dense web dashboard is compressed into a narrow mobile screen with multiple competing cards and unclear actions.

## After

The mobile version prioritizes one main action area, collapses secondary information, and uses screen-to-screen flow instead of trying to show the full desktop layout at once.
