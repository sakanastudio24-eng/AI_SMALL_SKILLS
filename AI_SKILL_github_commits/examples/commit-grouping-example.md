# Commit Grouping Example

## Diff

- auth session expiry fix
- UI cleanup on account page
- prettier formatting across unrelated files

## Better Grouping

1. `fix(auth): enforce session expiry on protected routes`
2. `refactor(account): simplify account page action layout`
3. leave broad formatting out of this branch or isolate it in its own commit
