# Bad Placeholder Example

## Problem
Temporary mock values are spread through multiple screens and shared hooks.

## Why It Fails
The eventual live integration requires cleanup across the whole app instead of a narrow replacement.
