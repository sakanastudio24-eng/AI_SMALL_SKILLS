# Route Handler Example

## Case

`app/api/projects/route.ts`

## Rule

Keep request parsing, auth checks, backend calls, and response shaping explicit. Do not bury route logic inside page components.
